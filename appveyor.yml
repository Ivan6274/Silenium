image: Ubuntu  

stack: jdk 11  

branches:
  only:
    - master  

build: off  

install:

  - gradlew test -Dselenide.headless=true
  - java -jar ./artifacts/app-order.jar &
  - chmod +x gradlew



build_script:
  - ./gradlew test --info  

